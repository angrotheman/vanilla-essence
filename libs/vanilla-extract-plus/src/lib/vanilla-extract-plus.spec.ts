import { vanillaExtractPlus } from './vanilla-extract-plus';

describe('vanillaExtractPlus', () => {
  it('should work', () => {
    expect(vanillaExtractPlus()).toEqual('vanilla-extract-plus');
  });
});
